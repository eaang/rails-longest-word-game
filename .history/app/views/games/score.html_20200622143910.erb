<h1>Result</h1>
<div class="results">
  <% if @from_grid %>
    This word is from the grid!
  <% else %>
    Sorry but <strong><%= @word.upcase %></strong> can't be built out of <%= @letters %>
  <% end %>
</div>


<p><%= link_to "Try again?", new_path %></p>